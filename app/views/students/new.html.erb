<h2>Add student:</h2>

<%= render 'form' %>

<h3>Current unordered roster:</h3>

<ul>
  <% @group.students.sort.each do |student| %>
    <li><%= student.last_name %>, <%= student.first_name %></li>
  <% end %>
</ul>

