<h2><%= @group.title %> show page</h2>

<h3>Class Roster</h3>
<ul>
  <% @students.each do |student| %>
    <li><%= student.last_name %>, <%= student.first_name %></li>
  <% end %>
</ul>

<%= link_to "Edit Roster", "/groups/#{@group.id}/students/roster_edit" %><br>
<%= link_to "Create Random groups", :small_groups %><br>
<%= link_to "Class settings", :class_settings %><br>
<a href="/groups">Back</a>