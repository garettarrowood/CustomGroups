<h2><%= @group.title %> show page</h2>

<h3>Class Roster</h3>
<ul>
  <% @students.each do |student| %>
    <li><%= student.last_name %>, <%= student.first_name %></li>
  <% end %>
</ul>

<%= link_to "Edit Roster", group_students_roster_edit_path(@group) %>