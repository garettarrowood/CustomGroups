<h2><%= @group.title %></h2>

<%= link_to "Create random groups", :small_groups %> | 
<%= link_to "Edit roster", group_edit_roster_path(@group) %> | 
<%= link_to "Class settings", :class_settings %> | 
<%= link_to "Back", groups_path %>

<h3>Class Roster</h3>
<ul>
  <% @group.alpha_students.each do |student| %>
    <li><%= student.last_name %>, <%= student.first_name %></li>
  <% end %>
</ul>